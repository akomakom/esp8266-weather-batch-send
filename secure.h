/** WIFI **/
typedef struct {
  const char *u;
  const char *p;
} wifi_login;


const wifi_login wifi_logins[] = {
  {"FIRSTSSID", "FIRSTPASSWORD"},
  {"SECONDSSID, "SECONDPASSWORD"}
  // and so on...
};